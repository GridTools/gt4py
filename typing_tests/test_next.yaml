  - case: fieldop
    main: |
      from __future__ import annotations
      import typing
      from gt4py import next as gtx

      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      @gtx.field_operator
      def foo(a: gtx.Field[gtx.Dims[KDim], gtx.int32]) -> gtx.Field[gtx.Dims[KDim], gtx.int32]:
          return a
      zeros = gtx.zeros({KDim: range(10)}, dtype=gtx.int32)
      more_zeros = foo(zeros)

  - case: fieldop_sig_with_kwargs
    main: |
      from gt4py import next as gtx

      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      @gtx.field_operator(grid_type=None, backend=None)
      def foo(a: gtx.Field[gtx.Dims[KDim], gtx.int32]) -> gtx.Field[gtx.Dims[KDim], gtx.int32]:
          return a

  - case: fieldop_sig_with_partial_kwargs
    main: |
      from gt4py import next as gtx

      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      @gtx.field_operator(grid_type=None)
      def foo(a: gtx.Field[gtx.Dims[KDim], gtx.int32]) -> gtx.Field[gtx.Dims[KDim], gtx.int32]:
          return a

  - case: scanop_sig_min
    main: |
      from gt4py import next as gtx

      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      @gtx.scan_operator(axis=KDim)
      def somescanop(
          carry: float,
          val: float,
      ) -> float:
          return carry + val

  - case: scanop_sig_with_kwargs
    main: |
      from gt4py import next as gtx

      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      @gtx.scan_operator(axis=KDim, forward=True, init=0.0)
      def somescanop(
          carry: float,
          val: float,
      ) -> float:
          return carry + val

  - case: program_sig
    main: |
      from __future__ import annotations
      import typing
      from gt4py import next as gtx

      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      @gtx.field_operator
      def foo(a: gtx.Field[gtx.Dims[KDim], gtx.int32]) -> gtx.Field[gtx.Dims[KDim], gtx.int32]:
          return a

      @gtx.program
      def foo_prg(
          a: gtx.Field[gtx.Dims[KDim], gtx.int32],
          b: gtx.Field[gtx.Dims[KDim], gtx.int32],
          start: gtx.int32,
          end: gtx.int32
      ) -> None:
          foo(a, out=b, domain={KDim: (start, end)})

  - case: program_sig_with_kwargs
    main: |
      from __future__ import annotations
      import typing
      from gt4py import next as gtx

      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      @gtx.field_operator
      def foo(a: gtx.Field[gtx.Dims[KDim], gtx.int32]) -> gtx.Field[gtx.Dims[KDim], gtx.int32]:
          return a

      @gtx.program(backend=None, grid_type=gtx.common.GridType.UNSTRUCTURED)
      def foo_prg(
          a: gtx.Field[gtx.Dims[KDim], gtx.int32],
          b: gtx.Field[gtx.Dims[KDim], gtx.int32],
          start: gtx.int32,
          end: gtx.int32
      ) -> None:
          foo(a, out=b, domain={KDim: (start, end)})

  - case: program_sig_with_partial_kwargs
    main: |
      from __future__ import annotations
      import typing
      from gt4py import next as gtx

      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      @gtx.field_operator
      def foo(a: gtx.Field[gtx.Dims[KDim], gtx.int32]) -> gtx.Field[gtx.Dims[KDim], gtx.int32]:
          return a

      @gtx.program(grid_type=None)
      def foo_prg(
          a: gtx.Field[gtx.Dims[KDim], gtx.int32],
          b: gtx.Field[gtx.Dims[KDim], gtx.int32],
          start: gtx.int32,
          end: gtx.int32
      ) -> None:
          foo(a, out=b, domain={KDim: (start, end)})

  - case: call_where
    main: |
      from __future__ import annotations
      import typing
      from gt4py import next as gtx

      class CellDim(gtx.DimensionIndex, kind=gtx.DimensionKind.HORIZONTAL): ...
      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...
      T = typing.TypeVar("T", float, gtx.float32, gtx.float64, bool, gtx.int32, gtx.int64)
      CellKField: typing.TypeAlias = gtx.Field[gtx.Dims[CellDim, KDim], T]

      def foo(
          mask: CellKField[bool], a: CellKField[gtx.float64], b: CellKField[gtx.float64]
      ) -> CellKField[gtx.float64]:
          tmp = gtx.where(mask & (a >= 0.0), b, 0.0)
          return gtx.where(mask, tmp, b)


  - case: call_where_tuples
    main: |
      from __future__ import annotations
      import typing
      from gt4py import next as gtx

      class CellDim(gtx.DimensionIndex, kind=gtx.DimensionKind.HORIZONTAL): ...
      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...
      T = typing.TypeVar("T", float, gtx.float32, gtx.float64, bool, gtx.int32, gtx.int64)
      CellKField: typing.TypeAlias = gtx.Field[gtx.Dims[CellDim, KDim], T]
      CKTuple: typing.TypeAlias = tuple[CellKField[T], CellKField[T]]

      def foo(
          mask: CellKField[bool], a: CKTuple[gtx.float64], b: CKTuple[gtx.float64]
      ) -> CKTuple[T]:
          return gtx.where(mask, a, b)

  - case: where_real_life
    main: |
      from __future__ import annotations
      import typing
      from gt4py import next as gtx
      from gt4py.next import where

      class CellDim(gtx.DimensionIndex, kind=gtx.DimensionKind.HORIZONTAL): ...
      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...
      T = typing.TypeVar("T", float, gtx.float32, gtx.float64, bool, gtx.int32, gtx.int64)
      CellField: typing.TypeAlias = gtx.Field[gtx.Dims[CellDim], T]
      CellKField: typing.TypeAlias = gtx.Field[gtx.Dims[CellDim, KDim], T]

      @gtx.field_operator
      def _postprocess_antidiffusive_cell_fluxes_and_min_max(
          refin_ctrl: CellField[gtx.int32],
          p_cc: CellKField[gtx.float64],
          z_tracer_new_low: CellKField[gtx.float64],
          z_tracer_max: CellKField[gtx.float64],
          z_tracer_min: CellKField[gtx.float64],
          lo_bound: gtx.int32,
          hi_bound: gtx.int32,
      ) -> tuple[
          CellKField[gtx.float64],
          CellKField[gtx.float64],
          CellKField[gtx.float64],
      ]:
          condition = (refin_ctrl == lo_bound) | (refin_ctrl == hi_bound)
          z_tracer_new_out = where(
              condition,
              gtx.minimum(1.1 * p_cc, gtx.maximum(0.9 * p_cc, z_tracer_new_low)),
              z_tracer_new_low,
          )

          z_tracer_max_out = where(
              condition, gtx.astype(gtx.maximum(p_cc, z_tracer_new_out), gtx.float64), z_tracer_max
          )
          z_tracer_min_out = where(
              condition, gtx.astype(gtx.minimum(p_cc, z_tracer_new_out), gtx.float64), z_tracer_min
          )

          return (z_tracer_new_out, z_tracer_max_out, z_tracer_min_out)

  - case: with_backend_fieldop
    main: |
      from __future__ import annotations
      import typing
      from gt4py import next as gtx

      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      @gtx.field_operator
      def foo(a: gtx.Field[gtx.Dims[KDim], gtx.int32]) -> gtx.Field[gtx.Dims[KDim], gtx.int32]:
          return a

      def foo_with_backend(backend: gtx.typing.Backend | None) -> None:
          _ = foo.with_backend(backend)

  - case: with_backend_scanop
    main: |
      from gt4py import next as gtx

      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      @gtx.scan_operator(axis=KDim)
      def somescanop(
          carry: float,
          val: float,
      ) -> float:
          return carry + val

      def somescanop_with_backend(backend: gtx.typing.Backend | None) -> None:
          _ = somescanop.with_backend(backend)

  - case: with_backend_program
    main: |
      from __future__ import annotations
      import typing
      from gt4py import next as gtx

      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      @gtx.field_operator
      def foo(a: gtx.Field[gtx.Dims[KDim], gtx.int32]) -> gtx.Field[gtx.Dims[KDim], gtx.int32]:
          return a

      @gtx.program
      def foo_prg(
          a: gtx.Field[gtx.Dims[KDim], gtx.int32],
          b: gtx.Field[gtx.Dims[KDim], gtx.int32],
          start: gtx.int32,
          end: gtx.int32
      ) -> None:
          foo(a, out=b, domain={KDim: (start, end)})

      def foo_prg_with_backend(backend: gtx.typing.Backend | None) -> None:
          _ = foo_prg.with_backend(backend)

  - case: astype_return_type
    main: |
      from __future__ import annotations
      import typing
      from gt4py import next as gtx

      class CDim(gtx.DimensionIndex, kind=gtx.DimensionKind.HORIZONTAL): ...
      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      @gtx.field_operator
      def foo(
          a: gtx.Field[gtx.Dims[CDim, KDim], gtx.float32],
          d: gtx.float32
      ) -> gtx.Field[gtx.Dims[CDim, KDim], gtx.float64]:
          z = gtx.astype(d * a, gtx.float64)
          return z

  - case: xarray_typing
    main: |
      import xarray
      a: xarray.NamedArray

  - case: class_style_dimension_in_field_annotation
    main: |
      from gt4py import next as gtx

      class IDim(gtx.DimensionIndex): ...
      class KDim(gtx.DimensionIndex, kind=gtx.DimensionKind.VERTICAL): ...

      a: gtx.Field[gtx.Dims[IDim, KDim], gtx.float64]
      reveal_type(a)
    out: |
      main:7:13: note: Revealed type is "gt4py.next.common.Field[tuple[main.IDim, main.KDim, fallback=gt4py.next.common.Dims[main.IDim, main.KDim]], float]"

  - case: class_style_dimension_mismatch_is_an_error
    main: |
      from gt4py import next as gtx

      class IDim(gtx.DimensionIndex): ...
      class JDim(gtx.DimensionIndex): ...

      def takes_i(f: gtx.Field[gtx.Dims[IDim], gtx.float64]) -> None: ...
      def pass_j(g: gtx.Field[gtx.Dims[JDim], gtx.float64]) -> None:
          takes_i(g)
    out: |
      main:8:13: error: Argument 1 to "takes_i" has incompatible type "Field[Dims[JDim], float]"; expected "Field[Dims[IDim], float]"  [arg-type]
      main:8:13: note: "Field[Dims[IDim], float].__call__" has type "def __call__(self, index_field: Connectivity[Any, Any] | FieldOffset, *args: Connectivity[Any, Any] | FieldOffset) -> Field[Any, Any]"

  - case: dimension_typevar_bound
    main: |
      from typing import TypeVar
      from gt4py import next as gtx

      D = TypeVar("D", bound=gtx.DimensionIndex)

      class IDim(gtx.DimensionIndex): ...

      def name_of(d: type[D]) -> str:
          return d.tag

      reveal_type(name_of(IDim))
    out: |
      main:11:13: note: Revealed type is "str"

  - case: index_is_an_instance_of_its_dimension
    main: |
      from gt4py import next as gtx

      class IDim(gtx.DimensionIndex): ...

      # no expected output: `IDim(0)` is an ordinary instance of `IDim`, and its position is
      # `.value`; the dimension's name is `IDim.tag`.
      idx: IDim = IDim(0)
      pos: int = IDim(0).value
      dim: gtx.Dimension = IDim(0).dim
      name: str = IDim.tag

  - case: index_of_the_wrong_dimension_is_an_error
    main: |
      from gt4py import next as gtx

      class IDim(gtx.DimensionIndex): ...
      class JDim(gtx.DimensionIndex): ...

      def takes_i(i: IDim) -> None: ...

      takes_i(JDim(0))
    out: |
      main:8:9: error: Argument 1 to "takes_i" has incompatible type "JDim"; expected "IDim"  [arg-type]

  - case: unparameterized_dims_still_accepts_any_rank
    main: |
      from gt4py import next as gtx

      def anything(f: gtx.Field[gtx.Dims, gtx.float64]) -> None: ...

      class IDim(gtx.DimensionIndex): ...
      class JDim(gtx.DimensionIndex): ...

      a: gtx.Field[gtx.Dims[IDim], gtx.float64]
      b: gtx.Field[gtx.Dims[IDim, JDim], gtx.float64]
      anything(a)
      anything(b)

  - case: programmatic_constructor_returns_a_dimension
    main: |
      from gt4py import next as gtx

      IDim = gtx.dimension("IDim")
      reveal_type(IDim)
    out: |
      main:4:13: note: Revealed type is "type[gt4py.next.common.DimensionIndex]"

  - case: dimension_alias_annotates_the_class_not_an_index
    main: |
      from gt4py import next as gtx

      class IDim(gtx.DimensionIndex): ...

      ok: gtx.Dimension = IDim
      bad: gtx.Dimension = IDim(0)
    out: |
      main:6:22: error: Incompatible types in assignment (expression has type "IDim", variable has type "type[DimensionIndex]")  [assignment]
